---
const today = new Date();
const podName = process.env.POD_NAME || '';
---

<footer>
	&copy; {today.getFullYear()} Made with love <span class="heart" title="❤">❤</span>
	<div class="social-links">
		<a href="https://www.linkedin.com/in/kalinind/" target="_blank" title="LinkedIn" aria-label="LinkedIn">
			<iconify-icon icon="mdi:linkedin" width="32" height="32"></iconify-icon>
		</a>

		<a href="https://kalinindanil17y.t.me/" title="Telegram" aria-label="Telegram">
			<iconify-icon icon="mdi:telegram" width="32" height="32"></iconify-icon>
		</a>

		<a href="https://www.facebook.com/profile.php?id=100018704474057" target="_blank" title="Facebook" aria-label="Facebook">
			<iconify-icon icon="mdi:facebook" width="32" height="32"></iconify-icon>
		</a>
		
		<a href="https://github.com/kalininDanil17Y" target="_blank" title="GitHub" aria-label="GitHub">
			<iconify-icon icon="mdi:github" width="32" height="32"></iconify-icon>
		</a>
	</div>
	<span>{podName}</span><br>
	<span><a href="https://github.com/kalininDanil17Y/kalinind.ru">Source code</a> | <a href="https://hub.docker.com/repository/docker/danilo9/kalinind.ru/">DockerHub</a></span>
</footer>

<style>
	footer {
		padding: 2em 1em 6em 1em;
		background: linear-gradient(var(--gray-gradient)) no-repeat;
		color: rgb(var(--gray));
		text-align: center;
		transition: background-color 0.3s ease, color 0.3s ease;
	}
	
	.social-links {
		display: flex;
		justify-content: center;
		gap: 1em;
		margin-top: 1em;
		align-items: center;
		flex-wrap: wrap;
	}
	
	.social-links a {
		text-decoration: none;
		color: rgb(var(--gray));
		transition: opacity 0.2s ease, color 0.3s ease;
	}
	
	.social-links a:hover {
		opacity: 0.7;
	}

	.footer-control-button {
		display: flex;
		align-items: center;
		gap: 0.3rem;
		padding: 0.4rem 0.8rem;
		background: rgba(var(--accent), 0.1);
		border: 1px solid rgba(var(--accent), 0.3);
		border-radius: 4px;
		color: var(--accent);
		cursor: pointer;
		font-size: 0.8rem;
		font-weight: 500;
		transition: all 0.2s ease;
		text-decoration: none;
		font-family: inherit;
	}

	.footer-control-button:hover {
		background: rgba(var(--accent), 0.2);
		border-color: rgba(var(--accent), 0.5);
	}

	:global(body.dark-theme) .footer-control-button {
		background: rgba(255, 255, 255, 0.1);
		border-color: rgba(255, 255, 255, 0.3);
		color: #e0e0e0;
	}

	:global(body.dark-theme) .footer-control-button:hover {
		background: rgba(255, 255, 255, 0.2);
		border-color: rgba(255, 255, 255, 0.5);
	}

	.heart {
		cursor: help;
	}

	footer a {
		color: rgb(var(--gray));
		transition: color 0.3s ease;
	}

	footer a:hover {
		color: var(--accent);
	}

	@media (max-width: 480px) {
		.social-links {
			gap: 0.5rem;
		}

		.footer-control-button {
			padding: 0.3rem 0.6rem;
			font-size: 0.75rem;
		}
	}
</style>

<script>
	import 'iconify-icon';
</script>

<script type="application/javascript" is:inline>
	window.addEventListener('load', function () {
		baguetteBox.run('.imgbox', {
			captions: function(element) {
				return element.getElementsByTagName('img')[0].alt;
			}
		});
  });
</script>
